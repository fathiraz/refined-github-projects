/**
 * Primer {@link registerPortalRoot} container name for the bulk-bar CSUI shadow mount.
 * SelectPanel / Overlay must portal inside the same shadow root as BaseStyles + styled-components.
 */
export const BULK_BAR_PRIMER_PORTAL_NAME = 'rgp-bulk-bar-primer-portal' as const
